# Config file for the rapic_ds package
# Defines the following variables:
#  RAPIC_DS_INCLUDE_DIRS - include directories for rapic_ds
#  RAPIC_DS_LIBRARIES    - libraries to link against

get_filename_component(RAPIC_DS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include("${RAPIC_DS_CMAKE_DIR}/rapic_ds-targets.cmake")
set(RAPIC_DS_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
set(RAPIC_DS_LIBRARIES rapic_ds)
